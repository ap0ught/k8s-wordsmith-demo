---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.web.name }}
spec:
  ports:
    - port: {{ .Values.web.port }}
      targetPort: {{ .Values.web.targetport }}
      name: {{ .Values.web.name }}
  selector:
    app: web
  type: LoadBalancer
